Instructions to run:

- Make sure Leap Motion daemon is running and detecting Leap Motion device:
	- In linux:  >sudo leapd
- Open personatrix.pd
- Open MamuLEDs-Pd.pd
- Run LeapMotionMamuLEDs-Pd. After that, the number of connections in [netreceive] must be one.
